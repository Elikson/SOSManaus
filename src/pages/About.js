import React from 'react'
import { View, Text } from 'react-native'

export default function About({ navigation }) {
    return (
        <View>
            <Text>About</Text>
        </View>
    )
}
